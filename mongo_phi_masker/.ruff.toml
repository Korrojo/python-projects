# Ruff configuration for mongo_phi_masker
# This is a legacy production codebase being integrated
# These rules are temporarily relaxed and will be fixed in PR #4

[lint]
# Ignore pre-existing legacy issues
ignore = [
    "E722",  # Do not use bare `except` - will fix in PR #4
    "B023",  # Function definition does not bind loop variable - will fix in PR #4
    "E402",  # Module level import not at top of file - will fix in PR #4
    "B007",  # Loop control variable not used - will fix in PR #4
    "F811",  # Redefinition of unused variable - will fix in PR #4
    "F821",  # Undefined name - will fix in PR #4
    "E721",  # Use `is` and `is not` for type comparisons - will fix in PR #4
]

# These will be removed in PR #4 when we:
# - Convert to Typer CLI
# - Fix all type errors
# - Integrate common_config properly
# - Apply comprehensive code quality improvements
