{
    "rules": [
        {
            "comment": "Placeholder rules for PatientReportFaxQueue collection",
            "comment2": "Update this file with actual PHI fields that need masking"
        },
        {"field": "PatientName", "rule": "random_uppercase_name", "params": null},
        {"field": "FaxNumber", "rule": "random_10_digit_number", "params": null},
        {"field": "ReportContent", "rule": "replace_string", "params": "xxxxxxxxxx"}
    ]
}
