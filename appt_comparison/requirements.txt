# Dependencies for appointment_comparison
# Install with: pip install -r requirements.txt

# Common config (shared library)
-e ../common_config

# CLI framework
typer

# MongoDB
pymongo

# CSV/Data processing
python-dateutil
