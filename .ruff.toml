# Ruff configuration for monorepo

[lint]
# Don't ignore errors globally - catch real issues

[lint.per-file-ignores]
# E402: Allow imports after code in run.py files only
# This is intentional pattern for sys.path manipulation in monorepo
"*/run.py" = ["E402"]

[lint.isort]
known-first-party = ["src", "common_config"]
